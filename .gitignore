# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# env files (can opt-in for committing if needed)
.env*

# turbo
.turbo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# typescript
*.tsbuildinfo

# Python service venvs
services/**/.venv/

# Local RAG vector store (JSON) — regenerated by re-ingesting documents
data/
**/data/rag/

# Vendored model weights (~3.4GB) — kept out of git (too big for GitHub's
# 100MB/file limit). Transferred to restricted machines via the offline zip.
/models/
/models-win/
*.zip
